//PUBLIC COMMAND

//this command outputs a random number between 1 and 10 of how festive the user is for the holiday
//usage: !F present
//output: 

/*exports.run = (client, message, args, user, channel, self, input, channelName) => {
    var festive = Math.floor((Math.random() * 10) + 1);
    if (festive === 0) {
        client.say(channel, `Uh oh! ${user.username} was naughty this year  ${festive} presents`);
    }
    client.say(channel, `${user.username} is ${festive} presents`);
}*/
